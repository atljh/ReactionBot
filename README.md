# TG Reacter

Мультиакаунтна система для масового проставлення реакцій в Telegram.

## Можливості

- Масове проставлення реакцій з кількох акаунтів
- Підтримка публічних та приватних каналів
- Підтримка .session+.json та tdata форматів
- Проксі для кожного акаунта
- SQLite база для відстеження реакцій
- Уникнення дублікатів
- Денні ліміти та затримки
- Перевірка підписки на закриті канали

## Встановлення

```bash
# Клонувати репозиторій
git clone https://github.com/atljh/tg_reacter.git
cd tg_reacter

# Створити віртуальне середовище
python -m venv venv

# Активувати віртуальне середовище
# Linux/macOS (bash/zsh)
source venv/bin/activate
# Windows (PowerShell)
.\\venv\\Scripts\\Activate.ps1
# Windows (cmd.exe)
venv\\Scripts\\activate.bat

# Встановити залежності
pip install -r requirements.txt

# Налаштувати конфіг
cp config/config.yaml.example config.yaml
# Відредагувати config.yaml
```

## Конфігурація

```yaml
api_id: YOUR_API_ID
api_hash: "YOUR_API_HASH"

sessions_dir: "./sessions"
database: "./database.db"

safety:
  max_reactions_per_day: 20
  min_account_age_days: 7
  delay_range: [5, 15]
```

## Використання

### Поставити реакції

Публічний канал:

```bash
python main.py \
  --link https://t.me/channel/123 \
  --reaction thumbsup \
  --count 50 \
  --threads 5 \
  --delay 5-15
```

Приватний канал / група (по ID + інвайт):

```bash
python main.py \
  --link https://t.me/c/1234567890/123 \
  --invite https://t.me/+INVITE_HASH \
  --reaction fire \
  --count 50 \
  --threads 5 \
  --delay 5-15
```

Пояснення:
- `--link` — посилання на конкретний пост (публічний або приватний)
- `--invite` — інвайт-лінк на приватний канал/групу, якщо акаунти ще не підписані
- Якщо акаунти вже в каналі, параметр `--invite` можна не передавати

### Параметри

| Параметр | Опис | За замовчуванням |
|----------|------|------------------|
| `--link` | Посилання на пост | - |
| `--reaction` | Тип реакції (thumbsup, heart, fire...) | thumbsup |
| `--count` | Кількість реакцій | 10 |
| `--threads` | Паралельні потоки | 5 |
| `--delay` | Затримка (сек) | 5-15 |
| `--dry-run` | Тестовий запуск | false |

### Реакції

```
thumbsup: 👍    thumbsdown: 👎    heart: ❤️
fire: 🔥        party: 🎉         hundred: 💯
cry: 😢         clap: 👏          think: 🤔
```

## Управління акаунтами

### Додати акаунт

```bash
python add_account.py --phone +380501234567 --proxy socks5://user:pass@ip:port
```

### Конвертувати tdata

```bash
python convert_tdata.py --tdata /path/to/tdata --proxy socks5://user:pass@ip:port
```

### Перевірити акаунти

```bash
python checker.py --all
```

### Синхронізувати сесії

```bash
python main.py --sync
```

### Статистика

```bash
python main.py --stats
```

## Структура проекту

```
tg_reacter/
├── config/
│   ├── settings.py
│   └── config.yaml.example
├── src/
│   ├── client.py          # Telethon клієнт
│   ├── database.py        # SQLite
│   ├── parser.py          # Парсинг посилань
│   ├── reactor.py         # Логіка реакцій
│   ├── session_loader.py  # Завантаження сесій
│   └── tdata_converter.py # Конвертер tdata
├── sessions/              # .session + .json файли
├── tdatas/                # tdata папки для конвертації
├── docs/
│   ├── 01_adding_accounts.txt
│   ├── 02_proxy_and_safety.txt
│   └── 03_usage_examples.txt
├── main.py
├── checker.py
├── add_account.py
├── convert_tdata.py
└── convert_all_tdata.py   # Масова конвертація tdata
```

## Формати акаунтів

### .session + .json

```
sessions/
├── +380501234567.session
└── +380501234567.json
```

### tdata

```
sessions/
└── +380501234567/
    └── tdata/
```

## Безпека

- Не більше 20 реакцій з акаунта на добу
- Затримки 5-30 секунд між діями
- Використовуйте проксі
- Не використовуйте нові акаунти (<7 днів)
- Див. `docs/02_proxy_and_safety.txt`

## Документація

- docs/01_adding_accounts.txt - Додавання акаунтів
- docs/02_proxy_and_safety.txt - Проксі та безпека
- docs/03_usage_examples.txt - Приклади використання

## Вимоги

- Python 3.9+
- Telethon >= 1.36
- telegram-desktop-decrypter (для конвертації tdata)
